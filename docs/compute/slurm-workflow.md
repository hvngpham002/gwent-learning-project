# Archived Slurm Workflow

> **CLAIM STATUS: INFRASTRUCTURE / ARCHIVAL REFERENCE**
> This file preserves the prior Slurm workflow. It is not an active execution
> guide. The [experiment status ledger](experiment-status-ledger.md) is the
> single source of truth and currently marks remote H100/Slurm access unavailable.

## Current Status

Do not run any command in this document while the ledger remains local-only.
All new experiments must use the local workflow and
`templates/local-batch.sh`. The material below is retained solely so a future
authorized setup change can be made deliberately; it does not grant access.

Historical server inventory is retained in `hardware-spec.md`. The former
submission templates are deliberately disabled, so they cannot start a remote
experiment by mistake. If remote access returns, add the new authorized setup
to the status ledger before replacing those placeholders.

## Claim Audit

Archival reference. No research claims.
