# Cluster F Phase 82 Specs - Search Consumer Casebook Decision

Date: 2026-06-27
Branch: `codex/cFp82-search-consumer-casebook-decision`
Base: `dev`

## Summary

cFp81 completed the first aggregate casebook over committed cFp80 compact
search-consumer action features:

- artifact family: `search-consumer-action-feature-casebook-v0/cFp81`;
- current suite: 3,979 cFp80 root rows, 18,820 compact action rows, 63
  inherited skipped roots, 80 casebook rows, 95 slice summaries;
- robust suite: 32,147 cFp80 root rows, 153,842 compact action rows, 340
  inherited skipped roots, 80 casebook rows, 95 slice summaries;
- current skipped-root percent: 1.56%;
- robust skipped-root percent: 1.05%;
- source consistency: `ready` for both suites;
- consumer readiness: `casebook_ready` for both suites;
- robust repeat hashes matched exactly;
- hidden-info exact-token and catalog-derived scans passed.

cFp81 did **not** choose, rank, evaluate, or score actions. It only described
coverage and readiness slices.

cFp82 is a decision phase. It should read the committed cFp81 casebook evidence
and decide the next safe benchmark-only search-consumer step.

## Decision To Make

cFp82 must choose exactly one of these paths and justify the choice from cFp81
evidence:

1. **Proceed to a small deterministic consumer-subset contract**.
   - Recommended if cFp81 shows enough `consumer_ready` coverage to support a
     bounded non-value consumer input surface.
   - This would become cFp83.
2. **Proceed directly to a broader feature-consumer contract**.
   - Only acceptable if cFp81 evidence shows broad readiness without dominant
     high-branching/high-collision pressure.
3. **Pause consumer work and return to instrumentation/casebook refinement**.
   - Recommended if cFp81 evidence is too sparse, too broad, or not specific
     enough to define a safe next contract.

The expected lean, based on cFp81, is option 1:

```text
cFp83: search-consumer-small-subset-contract-v0
```

The reason is that cFp81 shows a real consumer-ready slice, but most robust
action coverage is still high-branching or high-collision:

- robust `consumer_ready`: 24,405 actions / 9,580 roots;
- robust `consumer_ready_high_branching`: 85,651 actions / 19,094 roots;
- robust `consumer_ready_high_collision`: 43,629 actions / 23,338 roots;
- robust `consumer_ready_over_budget_context`: 157 actions / 45 roots.

This supports a bounded subset contract before any value, ranking, rollout, or
search-playing work.

## Scope

Implement cFp82 as a docs/metadata decision phase only.

Required outputs:

1. decision note:
   `docs/research/literature/ai/decisions/2026-06-27-cfp82-search-consumer-casebook-decision.md`;
2. audit report:
   `audit/reports/2026-06-27-cFp82-report.md`;
3. `docs/PROJECT_STATE.md` update marking cFp82 complete and naming cFp83 as
   the active recommended handoff;
4. AI Lab metadata update in `src/game/ai/productPolicies.ts`;
5. AI Lab metadata test update in
   `tests/components/gwent/authenticAiLabViewModel.test.ts`;
6. benchmark harness update in
   `docs/research/literature/ai/benchmark-harness.md`;
7. policy note update in
   `docs/research/literature/ai/policies/legal-heuristic-v1.md`;
8. roadmap update in `docs/overhaul-plan/ai-ml-roadmap.md`.

No new benchmark artifact family is required for cFp82. Do not add a CLI unless
you can justify it as a tiny read-only summarizer. Prefer deriving the cFp82
decision directly from the already committed cFp81 summaries, casebook rows,
slice summaries, and reports.

## Required Evidence To Read

Read these files before writing the decision:

- `docs/spec/2026-06-27-cFp81-specs.md`;
- `audit/reports/2026-06-27-cFp81-report.md`;
- `docs/research/literature/ai/decisions/2026-06-27-cfp81-search-consumer-action-feature-casebook.md`;
- current cFp81 summary:
  `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-v1/search-consumer-action-feature-casebook-v0/cFp81/summary.json`;
- robust cFp81 summary:
  `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-robust-v1/search-consumer-action-feature-casebook-v0/cFp81/summary.json`;
- current cFp81 casebook rows:
  `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-v1/search-consumer-action-feature-casebook-v0/cFp81/casebook-rows.jsonl`;
- robust cFp81 casebook rows:
  `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-robust-v1/search-consumer-action-feature-casebook-v0/cFp81/casebook-rows.jsonl`;
- current cFp81 slice summaries:
  `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-v1/search-consumer-action-feature-casebook-v0/cFp81/slice-summaries.jsonl`;
- robust cFp81 slice summaries:
  `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-robust-v1/search-consumer-action-feature-casebook-v0/cFp81/slice-summaries.jsonl`.

## Required Decision Note Content

The cFp82 decision note must include:

1. **Decision** - one clear selected path.
2. **Evidence From cFp81** - current and robust counts.
3. **Why Not Direct Search/Value Yet** - explain why high-branching and
   high-collision pressure block value/ranking/search claims.
4. **Why Not Pause Entirely** - explain why the consumer-ready slice is enough
   to justify a bounded next phase.
5. **Selected cFp83 Contract** - name the recommended next phase:
   `search-consumer-small-subset-contract-v0`.
6. **cFp83 Minimum Contract** - define what the next spec should do, without
   implementing it in cFp82.
7. **Explicit Non-Claims** - restate that cFp82 does not add search gameplay,
   values, rankings, reward labels, or product AI.
8. **Hidden-Info Safety** - state that cFp82 is docs/metadata only and does not
   emit new machine-readable hidden-info-bearing rows.

## Recommended cFp83 Minimum Contract

cFp82 should recommend that cFp83 build a benchmark-only small subset contract
with these constraints:

- consume committed cFp80/cFp81 artifacts read-only;
- produce a deterministic subset of roots/actions suitable for future
  consumer experiments;
- include only low-risk `consumer_ready` contexts unless the spec explicitly
  defines a separate diagnostic bucket;
- preserve cFp68/cFp69 skipped-root accounting;
- include exact source artifact references and row-count source consistency;
- emit scalar public-safe identifiers only;
- stay below small artifact targets;
- make no action-quality claim;
- compute no action values, expected values, rewards, win probabilities,
  payoffs, rankings, or best actions;
- execute no candidate actions;
- rebuild no sampled states;
- materialize no hidden worlds;
- change no engine, policy, sampler, deck, catalog, rating, or UI behavior.

## Explicit Out Of Scope

Do not implement:

- any new search-playing policy;
- PIMC action evaluation;
- ISMCTS/MCTS;
- rollouts;
- sampled-world rebuild;
- hidden-world materialization;
- action execution;
- action values;
- expected values;
- reward targets;
- win probabilities;
- payoff tables or payoff matrices;
- principal variations;
- action ordering;
- action ranking;
- best-action selection;
- training-data export;
- product-selectable search AI;
- AI Lab runner buttons;
- browser benchmark execution;
- gameplay behavior changes;
- `legal-heuristic-v1` behavior changes;
- `legal-heuristic-v0` behavior changes;
- engine rule changes;
- legal move changes;
- sampler behavior changes;
- benchmark suite definition changes;
- deck/catalog changes;
- rating formula/artifact changes;
- edits to cFp81 artifacts;
- edits to cFp80/cFp79/cFp78/cFp76 artifacts.

## Source Artifact Immutability

cFp82 must not modify files under these directories:

```text
docs/research/literature/ai/benchmark-results/*/search-consumer-action-feature-casebook-v0/cFp81/
docs/research/literature/ai/benchmark-results/*/search-consumer-action-features-dictionary-v0/cFp80/
docs/research/literature/ai/benchmark-results/*/search-consumer-action-features-v1/cFp79/
docs/research/literature/ai/benchmark-results/*/public-action-identities-compact-v0/cFp78/
docs/research/literature/ai/benchmark-results/*/search-consumer-action-features-v0/cFp76/
```

The implementation may read those artifacts but must treat them as immutable
inputs.

## AI Lab Metadata

Update `src/game/ai/productPolicies.ts` so legal-heuristic-v1 reports:

- `latestPhase: "cFp82"`;
- `latestSpecPath: "docs/spec/2026-06-27-cFp82-specs.md"`;
- `latestReportPath: "audit/reports/2026-06-27-cFp82-report.md"`;
- a capability entry stating cFp82 is a benchmark-only decision phase over the
  cFp81 casebook and recommends a small subset contract, not search gameplay.

Update `tests/components/gwent/authenticAiLabViewModel.test.ts` accordingly.

## Audit Report Requirements

Create `audit/reports/2026-06-27-cFp82-report.md`.

The report must include:

- files changed;
- behavior changed;
- evidence read;
- decision selected;
- cFp81 current/robust counts used;
- acceptance criteria status;
- tests/checks run;
- hidden-info scan result;
- deviations;
- risks/follow-ups;
- recommended next step;
- `Project State Update` section.

Because cFp82 is docs/metadata only, the report should say explicitly that no
new benchmark artifacts were generated and no existing artifact family was
modified.

## Hidden-Info Scan

Run a direct exact-token hidden-info scan over:

- `docs/spec/2026-06-27-cFp82-specs.md`;
- `docs/research/literature/ai/decisions/2026-06-27-cfp82-search-consumer-casebook-decision.md`;
- `audit/reports/2026-06-27-cFp82-report.md`;
- updated docs/metadata files.

Use exact-token matching for unsafe runtime fields and do not paste raw hazard
tokens unnecessarily into the report or decision note. If the scan list itself
would trigger the scan, describe it in sanitized prose instead of writing the
literal forbidden strings in cFp82 docs.

## Required Checks

Run at minimum:

```bash
npm test -- --run tests/components/gwent/authenticAiLabViewModel.test.ts
npm run typecheck
npm run lint
npm run build
npm run check:forbidden-imports
npm run check:project-state -- --base origin/dev
git diff --check
git diff --cached --check
npm run check:worktree-clean
```

Also run an exact-token hidden-info scan as described above.

Full `npm test` is optional for cFp82 because the phase is docs/metadata only
and should not alter runtime behavior. If you do not run it, say that clearly
in the report.

## Acceptance Criteria

- cFp82 decision note exists and selects exactly one path.
- cFp82 selects a next phase based on cFp81 evidence, not intuition.
- cFp82 recommends cFp83
  `search-consumer-small-subset-contract-v0` unless the evidence read during
  implementation contradicts the expected cFp81 readout.
- AI Lab metadata is updated to cFp82.
- `docs/PROJECT_STATE.md` marks cFp82 complete and cFp83 as the active next
  handoff.
- Benchmark harness, policy doc, and roadmap mention cFp82.
- No cFp81/cFp80/cFp79/cFp78/cFp76 artifact directories are modified.
- No source code that changes gameplay, policy behavior, sampler behavior,
  engine behavior, or benchmark artifact generation semantics is changed.
- Hidden-info scan passes.
- Required checks pass.
- Worktree is clean after commit.

## Copy-Ready Prompt For Coder

You are on `dev` in `/Users/viethungpham/Documents/GitHub/gwent-learning-project`.

Implement only `docs/spec/2026-06-27-cFp82-specs.md`.

Create branch:

```bash
git checkout dev
git pull --ff-only origin dev
git checkout -b codex/cFp82-search-consumer-casebook-decision
```

Read first:

1. `AGENTS.md`
2. `docs/PROJECT_STATE.md`
3. `docs/spec/2026-06-27-cFp82-specs.md`
4. `audit/reports/2026-06-27-cFp81-report.md`
5. `docs/research/literature/ai/decisions/2026-06-27-cfp81-search-consumer-action-feature-casebook.md`

This is a docs/metadata decision phase only. Do not add search gameplay, values,
rankings, rewards, rollouts, product AI, benchmark runners, or new generated
artifact families. Do not modify cFp81/cFp80/cFp79/cFp78/cFp76 artifact
directories.

Expected decision: recommend cFp83
`search-consumer-small-subset-contract-v0`, justified by cFp81 evidence showing
a usable `consumer_ready` slice but dominant high-branching/high-collision
pressure.

Required deliverables:

- cFp82 decision note;
- cFp82 audit report;
- AI Lab metadata and test update;
- benchmark harness, policy doc, roadmap, and `PROJECT_STATE.md` updates;
- hidden-info scan;
- required checks from the spec;
- commit and push the branch.
