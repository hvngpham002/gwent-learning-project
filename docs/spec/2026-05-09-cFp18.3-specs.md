# cFp18.3 Specification - Brown ReBeL Annotation Finalization

## Goal

Finalize the `brown2020rebel` annotation after explicit user
finalization permission. The full local source was already read in
`cFp18`, `cFp18.1`, and `cFp18.2` with `pages_read: "1-25"`; this
phase adds only the final Gwent relevance, adaptation-risk, and
AI-roadmap decision sections.

## Inputs

- `AGENTS.md`
- `docs/PROJECT_STATE.md`
- `docs/research/literature/ai/ANNOTATION_WORKFLOW.md`
- `docs/research/literature/ai/source/brown2020rebel/manifest.yaml`
- `docs/research/literature/ai/annotations/brown2020rebel/brown2020rebel.md`
- `audit/reports/2026-05-09-cFp18.2-report.md`

## Scope

- Preserve `pages_read: "1-25"` for `brown2020rebel`.
- Set the annotation status to `complete`.
- Complete `Relevance To Gwent AI`, `Risks For Adaptation`, and
  `AI-Roadmap Decision` using only already-read page-grounded notes.
- Update the literature queue entry for `brown2020rebel` to
  `complete`.
- Update `docs/PROJECT_STATE.md` with the phase handoff, completed
  ledger row, current risks, and next-step wording.
- Create the required phase report.

## Out Of Scope

- No new PDF page reading.
- No broad literature synthesis.
- No finalization of `brown2019deepcfr`.
- No AI implementation, runtime code, engine rule, simulation, UI,
  catalog, route, package, CI, or generated-asset behavior changes.
- No ReBeL, Deep CFR, NFSP, OSFP, ISMCTS, or model-training
  implementation spec.

## Acceptance Criteria

- `brown2020rebel` frontmatter has `status: "complete"` and
  `pages_read: "1-25"`.
- The three final decision sections are no longer pending and include
  explicit Gwent roadmap decisions.
- The queue marks `brown2020rebel` complete.
- `docs/PROJECT_STATE.md` points the active Cluster F handoff at this
  phase and preserves the existing `cFp19` Batch A decision note.
- The report documents files changed, behavior changed, checks run,
  acceptance criteria, deviations, risks/follow-ups, project-state
  update, and recommended next step.

## Required Checks

- `git diff --check`
- `npm run check:project-state -- --base HEAD~1`
