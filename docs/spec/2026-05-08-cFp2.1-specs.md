# cFp2.1 Specs - Second Cowling ISMCTS Annotation Batch

## Phase

Cluster F Phase 2.1: Second Page-Bounded Cowling Annotation Batch

## Goal

Continue the `cowling2012ismcts` annotation only after explicit user
permission by reading the second page batch, PDF pp. 11-20, and
appending page-grounded notes immediately. This phase still does not
make the final AI-roadmap decision because the paper remains partially
unread.

## Inputs

Read:

- `AGENTS.md`
- `docs/PROJECT_STATE.md`
- `docs/research/literature/ai/ANNOTATION_WORKFLOW.md`
- `docs/research/literature/ai/source/cowling2012ismcts/manifest.yaml`
- `docs/research/literature/ai/annotations/cowling2012ismcts/cowling2012ismcts.md`

## Scope

1. Confirm the existing annotation has `pages_read: "1-10"`.
2. Read only PDF pp. 11-20 of
   `docs/research/literature/ai/source/cowling2012ismcts/source.pdf`.
3. Append page-grounded notes for the pages just read.
4. Update the annotation frontmatter to `pages_read: "1-20"`.
5. Keep `Relevance To Gwent AI`, `Risks For Adaptation`, and
   `AI-Roadmap Decision` pending until the full paper is read.
6. Update `docs/PROJECT_STATE.md`.
7. Create `audit/reports/2026-05-08-cFp2.1-report.md`.

## Out Of Scope

- No AI implementation code.
- No engine, UI, AI policy, simulation, catalog, test, route, CI, or
  package changes.
- No reading past PDF p. 20 in this phase.
- No final Cluster F AI method decision.
- No claims from raw model sweep outputs.

## Reading Rules

- Follow the mandatory batch protocol in
  `docs/research/literature/ai/ANNOTATION_WORKFLOW.md`.
- Stop after writing notes for pp. 11-20.
- Do not continue to PDF p. 21 unless the user explicitly replies with
  a continuation instruction.
- Every factual paper claim in the annotation must include a page or
  section anchor.
- Project-specific inferences must be labeled `Project inference:`.

## Required Checks

- `git diff --check`
- `npm run check:project-state -- --base HEAD~1`

## Reporting Directive

Create:

- `audit/reports/2026-05-08-cFp2.1-report.md`

The report must include files changed, behavior changed, checks run,
acceptance criteria status, deviations, risks/follow-ups, Project State
Update, and recommended next step.
