# cFp41.2 Specs: Expanded Benchmark Artifact Refresh

Date: 2026-05-20

## Summary

Regenerate and commit the post-cFp41.1 expanded benchmark artifact boundary for
`legal-heuristic-v1`. This is an evaluation/artifact refresh only. It must not
change AI policy behavior, engine rules, legal moves, UI behavior, catalog data,
deck presets, benchmark suite shape, ratings, search, training, or product
difficulty.

## Context

cFp41 added `benchmark-v1-starter-matrix-expanded-v1`, a 400-record discovery
suite. The first expanded run exposed a cFp38/cFp39 recursive-scoring stack
overflow. cFp41.1 fixed that stack overflow. cFp41.2 records the regenerated
expanded artifact boundary after the fix and aligns AI Lab/product metadata with
the exact result.

## Required Work

1. Run or consume a fresh successful expanded profile:

   ```bash
   npm run benchmark:long -- --profile v1-expanded
   ```

2. Commit the regenerated expanded starter-matrix artifacts under:

   ```text
   docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-expanded-v1/latest/
   ```

   Required files:

   - `manifest.json`
   - `summary.json`
   - `records.jsonl`
   - `report.md`

3. Commit the regenerated expanded failure-mining artifacts under:

   ```text
   docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-expanded-v1/failure-mining/latest/
   ```

   Required files:

   - `manifest.json`
   - `summary.json`
   - `findings.jsonl`
   - `report.md`
   - `tuning-queue.md`

4. Update product AI metadata in `src/game/ai/productPolicies.ts` so
   `legal-heuristic-v1` reports:

   - latest phase: `cFp41.2`
   - spec path: `docs/spec/2026-05-20-cFp41.2-specs.md`
   - report path: `audit/reports/2026-05-20-cFp41.2-report.md`
   - expanded result: `310 win / 85 loss / 5 draw vs v0; 0 policy failures`

5. Update `/ai-lab` unit expectations to match the product policy metadata.

6. Update `docs/research/literature/ai/policies/legal-heuristic-v1.md` with the
   cFp41.2 expanded artifact result and failure-mining totals.

7. Update `docs/research/literature/ai/benchmark-results/README.md` with a short
   note for the latest committed expanded artifact refresh.

8. Update `docs/PROJECT_STATE.md` with Last Updated, Completed Phase Ledger,
   Latest Audit, and Next Recommended Step entries.

9. Create `audit/reports/2026-05-20-cFp41.2-report.md`.

## Acceptance Criteria

- `benchmark-v1-starter-matrix-expanded-v1/latest/summary.json` reports:
  - 400 total matches
  - 400 completed
  - 0 policy failures
  - 0 engine errors
  - 0 replay failures
  - `legal-heuristic-v1`: 310 wins / 85 losses / 5 draws
- Expanded failure mining reports:
  - 400 records
  - 964 findings
  - `suspicious_pass=717`
  - `round_three_low_resource=97`
  - `weathered_row_play=86`
  - `round_one_overinvestment=51`
  - `medic_timing_risk=11`
  - `policy_failed=0`
- `tuning-queue.md` uses `Recommended Next Scope`, not stale cFp36-specific
  wording.
- `/ai-lab` metadata is not stale relative to cFp41.2.
- No runtime code changes other than product policy metadata.
- No `.local/benchmark-runs/` files are committed.

## Out Of Scope

- AI policy scoring or constants.
- Engine rules, legal moves, command execution, prompts, or scoring.
- UI behavior or route changes beyond static AI Lab metadata.
- Catalog records, deck presets, Card Studio, or deck builder data.
- Benchmark suite shape or seed count changes.
- Ratings, search, self-play, model training, H100/Slurm execution, or product
  difficulty.

## Required Checks

Run:

```bash
npm test -- --run tests/components/gwent/authenticAiLabViewModel.test.ts tests/game/benchmarkHarness.test.ts tests/game/benchmarkArtifacts.test.ts
npm test
npm run typecheck
npm run lint
npm run build
npm run check:forbidden-imports
npm run check:project-state -- --base HEAD~1
git diff --check
```

Also run a hidden-info hazard scan over the expanded artifact directory. Exact
implementation may reuse the existing artifact-test hazard set; the scan must
not find raw state, command logs, private hand/deck arrays, unsafe debug results,
or runtime card instance IDs.

## Copy-Ready Prompt

Not applied at this stage; cFp41.2 is implemented directly as a narrow artifact
refresh.
