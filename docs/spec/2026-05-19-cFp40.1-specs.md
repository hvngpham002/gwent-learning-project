# Cluster F Phase 40.1 - Benchmark Artifact Refresh After Long Runner

## Purpose

Commit the benchmark artifact refresh produced by the new cFp40 long-run
wrapper and align all user-facing benchmark totals with the refreshed
artifacts.

This phase is benchmark hygiene only. It must not change AI policy
behavior, engine rules, legal moves, UI behavior, catalog data, deck
presets, benchmark suite definitions, ratings, search, training, or
product difficulty.

## Source Run

User-run command:

```bash
npm run benchmark:long -- --profile v1-current
```

Run bundle:

```text
.local/benchmark-runs/20260519-042207-305-v1-current/
  summary.md
  run.json
  01-v1-smoke-comparison.log
  02-v1-starter-matrix.log
  03-v1-failure-mining.log
```

## Refreshed Totals

- `benchmark-v1-smoke-v1`: 10 win / 2 loss / 0 draw vs v0.
- `benchmark-v1-starter-matrix-v1`: 104 win / 14 loss / 2 draw vs v0.
- Failure mining: 296 findings:
  - suspicious_pass = 225;
  - round_three_low_resource = 34;
  - weathered_row_play = 22;
  - round_one_overinvestment = 9;
  - medic_timing_risk = 3;
  - matchup_skew = 2;
  - deck_skew = 1.

## Required Work

1. Commit the regenerated benchmark artifacts under:
   - `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-v1/latest/`
   - `docs/research/literature/ai/benchmark-results/benchmark-v1-starter-matrix-v1/failure-mining/latest/`
2. Update `src/game/ai/productPolicies.ts` so `/ai-lab` reports the refreshed
   starter matrix total.
3. Update AI Lab tests that assert benchmark totals.
4. Update `docs/research/literature/ai/policies/legal-heuristic-v1.md`.
5. Update `docs/PROJECT_STATE.md`.
6. Create this phase report.

## Acceptance Criteria

- AI Lab metadata reports 104 win / 14 loss / 2 draw for v1 starter matrix.
- v1 policy docs report 296 failure-mining findings and 3 Medic timing risks.
- Project state recommends the next expanded automated playtest phase from the
  refreshed baseline.
- No source files under `src/game/ai/legalHeuristicPolicyV1.ts` or benchmark
  suite definitions are changed.
- Standard checks pass.
