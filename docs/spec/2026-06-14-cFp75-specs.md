# Cluster F Phase 75 Specs - Search Consumer Contract Decision

Date: 2026-06-14
Branch: `codex/cFp75-search-consumer-contract-decision`
Base: `dev`

## Summary

cFp74 completed the benchmark-only bounded second-ply public-action scaffold.
It proved the existing sampled-world path can execute a second public action
layer inside cloned benchmark states with explicit cap accounting and hidden
information safety:

- current: 3,593 in-cap roots, 386 over-budget roots, 63 inherited skipped
  roots, 628,847 completed second-ply pairs, 0 failed/deferred pairs;
- robust: 28,915 in-cap roots, 3,232 over-budget roots, 340 inherited skipped
  roots, 5,101,445 completed second-ply pairs, 0 failed/deferred pairs;
- robust `second-ply-roots.jsonl`: 58,287,850 bytes after compact artifact
  projection; and
- robust repeat hashes matched exactly.

cFp75 should not extend directly to a third-ply execution probe. The evidence
now says the mechanics work, but the artifact/cost curve is real. The next
phase should define the first concrete hidden-info-safe search-readiness
consumer: what data it needs, what artifact shape it should consume, what it is
allowed to claim, and what cFp76 should implement.

cFp75 is therefore a decision/spec implementation phase. It should produce a
compact decision note and report, update project state and AI Lab metadata, and
recommend the next executable phase. It should not run new game-command
benchmark layers, generate large JSONL probe artifacts, rank actions, estimate
value, or wire anything into product AI.

## Decision For cFp75

cFp75 should make this decision explicit:

> Pause blind depth expansion. Do not implement a third-ply public-action
> execution probe yet. The next executable phase should be a benchmark-only
> public-action feature consumer contract, starting with compact per-root /
> per-public-action scalar features that future PIMC/ISMCTS/evaluator code can
> consume.

The cFp76 target should be:

> `search-consumer-action-features-v0`: a compact benchmark-only artifact that
> converts existing cFp68-cFp74 readiness evidence into hidden-info-safe
> per-root and per-root-public-action feature rows. It must not choose actions,
> rank actions, estimate values, run rollouts, or change gameplay policy.

This is the bridge between "we can execute sampled-world scaffolds" and "we can
build a search/evaluation consumer." Without this bridge, a third-ply probe
mostly creates more depth evidence without answering how any future search
policy will use it.

## Why This Phase Exists

The current ladder has intentionally avoided search-strength claims:

- cFp68: valid-root-only determinized probe contract with explicit skip
  accounting.
- cFp69: public-action root scaffold.
- cFp70: sampled-world action availability.
- cFp71: one-ply public-action outcome skeleton.
- cFp72: post-one-ply next-surface branching budget.
- cFp73: scalar casebook over cFp72 branching pressure.
- cFp74: bounded second-ply public-action scaffold.

cFp74 is the first phase that executes a second public action layer. It is
mechanically successful, but the robust run already completes more than five
million second-ply pairs and writes a large compact JSONL artifact. A third-ply
execution probe would likely multiply artifacts and compute before the project
has a consumer that says which features matter.

cFp75 should stop that drift. It should make the search-readiness consumer
contract legible before deeper execution exists.

## Prior Phase Readout

cFp74 current starter matrix:

- branching roots: 3,979.
- in-cap roots: 3,593.
- over-budget roots: 386.
- inherited skipped roots: 63.
- planned second-ply pairs total: 863,479.
- planned second-ply pairs in cap: 628,847.
- planned second-ply pairs over budget: 234,632.
- completed second-ply pairs: 628,847.
- failed/deferred second-ply pairs: 0.
- over-budget reason: `root_second_ply_pair_cap_exceeded` = 386.
- source consistency: ready.
- in-cap roots not observed: 0.
- `second-ply-roots.jsonl`: 7,169,242 bytes.

cFp74 robust starter matrix:

- branching roots: 32,147.
- in-cap roots: 28,915.
- over-budget roots: 3,232.
- inherited skipped roots: 340.
- planned second-ply pairs total: 7,095,113.
- planned second-ply pairs in cap: 5,101,445.
- planned second-ply pairs over budget: 1,993,668.
- completed second-ply pairs: 5,101,445.
- failed/deferred second-ply pairs: 0.
- over-budget reason: `root_second_ply_pair_cap_exceeded` = 3,232.
- source consistency: ready.
- in-cap roots not observed: 0.
- `second-ply-roots.jsonl`: 58,287,850 bytes.

cFp74 repair readout:

- Repair 1 fixed public-transfer memory continuity by priming eligible
  over-budget roots before returning from the root observer.
- Repair 2 compacted only `second-ply-roots.jsonl` artifact rows by moving
  repeated run/cap constants into manifest/summary and omitting zero-valued
  second-ply count-map entries.
- Default caps stayed unchanged.
- Robust coverage was not lowered.
- The 90 MB guard stayed unchanged.

## Scope

Implement cFp75 as a docs/metadata decision phase that:

1. reads the cFp74 report and decision note;
2. reads cFp74 current and robust `summary.json` plus `manifest.json`;
3. optionally inspects artifact file sizes and hash fields, but does not
   regenerate cFp74 artifacts;
4. writes a cFp75 decision note that explains why third-ply execution is paused;
5. defines `search-consumer-action-features-v0` as the recommended cFp76 target;
6. defines the minimum action-feature consumer schema for cFp76;
7. defines cFp76 source artifact contracts and source consistency checks;
8. defines cFp76 budget/size limits and stop conditions;
9. defines cFp76 hidden-info safety rules;
10. defines cFp76 non-claims;
11. updates AI Lab metadata to cFp75 as search-consumer planning
    infrastructure, not search gameplay;
12. updates the AI Lab metadata test;
13. updates benchmark/search docs and roadmap text;
14. updates `docs/PROJECT_STATE.md`; and
15. creates the required cFp75 audit report.

cFp75 may add small helper text or documentation tables. It should not add new
benchmark runtime modules unless needed only for metadata typing. Prefer
docs-only plus AI Lab metadata/test updates.

## Explicit Out Of Scope

Do not implement:

- third-ply action surface counting;
- third-ply command execution;
- new benchmark root re-observation;
- sampled-world rebuild;
- hidden multiset materialization;
- rollout search;
- PIMC action evaluation;
- ISMCTS/MCTS;
- action values;
- expected value;
- reward targets;
- win probability;
- payoff tables;
- principal variations;
- action ordering;
- action ranking;
- best-action selection;
- search-selected move choice;
- policy replacement;
- v1 heuristic behavior changes;
- v0 behavior changes;
- product-selectable search AI;
- AI Lab runner buttons;
- browser benchmark execution;
- difficulty tiers;
- training data export;
- Python notebooks;
- deck/catalog edits;
- engine rules or legal move changes;
- benchmark suite definition changes;
- rating formula/artifact changes;
- UI runtime behavior changes;
- changes to existing cFp68/cFp69/cFp70/cFp71/cFp72/cFp73/cFp74 artifacts.

cFp75 is not a benchmark execution phase. It is a decision and contract phase.

## Required Decision Note

Create:

- `docs/research/literature/ai/decisions/2026-06-14-cfp75-search-consumer-contract.md`

The decision note must include these sections:

1. `Decision`
2. `Why Not Third-Ply Yet`
3. `Evidence From cFp74`
4. `First Search-Consumer Target`
5. `cFp76 Artifact Contract`
6. `cFp76 Feature Schema`
7. `cFp76 Source Consistency`
8. `cFp76 Hidden-Info Safety`
9. `cFp76 Budgets And Stop Conditions`
10. `Non-Claims`
11. `Recommended Next Step`

The decision section must state:

- cFp75 pauses blind third-ply execution;
- cFp76 should implement `search-consumer-action-features-v0`;
- cFp76 should be benchmark-only and hidden-info-safe; and
- cFp76 must not make search-strength claims.

## cFp76 Target Contract

The cFp75 decision note must specify this cFp76 target.

Artifact family:

- `search-consumer-action-features-v0/cFp76/`

Suites:

- `benchmark-v1-starter-matrix-v1`
- `benchmark-v1-starter-matrix-robust-v1`

Required files per suite:

- `manifest.json`
- `summary.json`
- `root-features.jsonl`
- `action-features.jsonl`
- `skipped-roots.jsonl`
- `report.md`

Recommended source artifacts:

- cFp68 eligible/skipped roots;
- cFp69 public-action root scaffold;
- cFp70 sampled-world action availability;
- cFp71 one-ply outcome skeleton;
- cFp72 post-one-ply branching budget;
- cFp73 branching budget casebook;
- cFp74 bounded second-ply scaffold.

cFp76 should not re-execute commands if it can derive the first action-feature
contract from committed scalar artifacts. If per-action features require
additional observation, the cFp75 note must require a stop-and-redesign before
adding game-command execution.

## cFp76 Feature Schema

The decision note must define a minimum hidden-info-safe schema. cFp76 may
refine exact field names, but it must preserve the categories below.

Root-level feature row:

- suite id;
- matchup id;
- seed;
- mirror group/index when present;
- step;
- decision index;
- phase;
- round;
- seat id;
- policy id;
- faction;
- deck preset id;
- root public fingerprint;
- cFp68 eligibility/skipped status;
- cFp69 root public action count;
- cFp70 availability disagreement flag/count;
- cFp71 one-ply pair count and divergence flag/count;
- cFp72 post-one-ply budget totals/buckets;
- cFp73 casebook labels when present;
- cFp74 cap status and over-budget reason when present;
- cFp74 completed/failed/deferred second-ply pair counts when present;
- inherited skipped-root counters;
- consumer readiness status.

Action-level feature row:

- deterministic public action reference, not raw move id;
- public action kind;
- target kind;
- target side;
- public row/zone bucket when applicable;
- target expansion bucket;
- sampled availability status/count from cFp70 if available;
- one-ply transition counts from cFp71 if available;
- post-one-ply budget contribution from cFp72 if available;
- second-ply completion/contribution bucket from cFp74 if available;
- root/cap/skipped linkage;
- no card name, source id, instance id, raw move, command, event, final state,
  sampled hand, or sampled deck payload.

If existing committed artifacts cannot populate action-level rows without
adding new game-command execution, cFp76 must explicitly stop and report
`action_feature_source_gap` rather than inventing features.

## cFp76 Budget Guidance

The decision note must require cFp76 to keep artifacts compact:

- robust `action-features.jsonl` target: below 50 MB;
- hard stop if any single generated artifact reaches 90 MB before writing;
- no gzip/binary artifact workaround;
- if robust action features exceed the target, prefer a deterministic casebook
  subset over widening rows;
- no full command/event/state payloads;
- sparse count maps where applicable;
- repeated run/source constants belong in manifest/summary, not every row.

## Hidden-Info Safety

The cFp75 decision note must explicitly ban these payloads from cFp76:

- `cardsById`;
- `finalState`;
- `commandLog`;
- `eventLog`;
- `ownHand`;
- `opponentHand`;
- `unsafeDebugResults`;
- `decisionTrace`;
- `moveId`;
- `sourceCardId`;
- `sourceId`;
- `cardId`;
- `actionRef`;
- `rawMove`;
- `rawLabel`;
- `deckOrder`;
- `sampledHand`;
- `sampledDeck`;
- runtime instance prefixes such as `seat_a:` and `seat_b:`;
- card names and leader names in generated artifacts, unless they are part of
  prose docs rather than machine-readable artifact rows.

cFp75 itself must run a direct exact-token hidden-info scan over the cFp75
decision note and report. If product policy metadata or docs mention normal
prose terms, keep the scan scoped to machine-dangerous tokens and cFp75 output
paths to avoid false positives.

## AI Lab Update

Update `src/game/ai/productPolicies.ts` so legal-heuristic-v1 metadata points
to cFp75 after implementation:

- `latestPhase: "cFp75"`
- `latestSpecPath: "docs/spec/2026-06-14-cFp75-specs.md"`
- `latestReportPath: "audit/reports/2026-06-14-cFp75-report.md"`

The description/capabilities should say cFp75 is:

- search-consumer contract planning;
- benchmark infrastructure;
- not search gameplay;
- no runner;
- no policy behavior change.

Update `tests/components/gwent/authenticAiLabViewModel.test.ts` accordingly.

Do not add AI Lab buttons, controls, file pickers, or benchmark runners.

## Docs To Update

Update:

- `docs/PROJECT_STATE.md`
- `docs/research/literature/ai/benchmark-harness.md`
- `docs/research/literature/ai/policies/legal-heuristic-v1.md`
- `docs/overhaul-plan/ai-ml-roadmap.md`

Create:

- `docs/research/literature/ai/decisions/2026-06-14-cfp75-search-consumer-contract.md`
- `audit/reports/2026-06-14-cFp75-report.md`

The report must include:

- files changed;
- behavior changed;
- tests/checks run;
- acceptance criteria status;
- deviations;
- risks/follow-ups;
- recommended next step;
- `Project State Update` section.

## Tests And Checks

Required checks:

```bash
npm test -- --run tests/components/gwent/authenticAiLabViewModel.test.ts
npm run typecheck
npm run lint
npm run build
npm run check:forbidden-imports
npm run check:project-state -- --base origin/dev
git diff --check
git diff --cached --check
npm run check:worktree-clean
```

Run full `npm test` if any runtime TypeScript module outside
`src/game/ai/productPolicies.ts` changes.

Run `npm run ci:browser` only if browser/UI runtime source changes. Metadata
and AI Lab test updates alone do not require it.

Also run direct exact-token hidden-info scan over:

- `docs/research/literature/ai/decisions/2026-06-14-cfp75-search-consumer-contract.md`
- `audit/reports/2026-06-14-cFp75-report.md`

Suggested scan:

```bash
rg -n --pcre2 "\\b(cardsById|finalState|commandLog|eventLog|ownHand|opponentHand|unsafeDebugResults|decisionTrace|moveId|sourceCardId|sourceId|cardId|actionRef|rawMove|rawLabel|deckOrder|sampledHand|sampledDeck)\\b|seat_a:|seat_b:" docs/research/literature/ai/decisions/2026-06-14-cfp75-search-consumer-contract.md audit/reports/2026-06-14-cFp75-report.md
```

This scan should return no matches. If prose needs to mention banned payload
names, use a separate "excluded payload" paragraph and document the false
positive carefully; otherwise prefer avoiding the exact tokens in cFp75 prose.

## Acceptance Criteria

cFp75 is complete only if:

- cFp75 decision note exists;
- cFp75 decision note clearly pauses third-ply execution;
- cFp75 defines `search-consumer-action-features-v0` as the recommended cFp76
  target;
- cFp75 defines cFp76 source artifacts, output files, feature schema, budget
  limits, hidden-info restrictions, and non-claims;
- AI Lab metadata points at cFp75 and describes it as planning infrastructure,
  not search gameplay;
- docs and project state are updated;
- cFp75 report exists and includes the required sections;
- hidden-info scan passes over cFp75 decision/report files;
- required checks pass;
- worktree is clean;
- branch is committed and pushed.

## Expected cFp76 Decision Logic

Use this guidance in the cFp75 decision note:

- If committed cFp68-cFp74 artifacts contain enough public-action linkage to
  derive per-action feature rows without re-executing commands, cFp76 should
  implement `search-consumer-action-features-v0` directly from committed
  artifacts.
- If action-level linkage is missing, cFp76 should stop with an
  `action_feature_source_gap` casebook/decision note rather than widening scope
  into new command execution.
- If robust action-feature artifacts exceed the size target, cFp76 should
  switch to a deterministic casebook subset instead of adding compression or
  trimming evidence silently.
- cFp76 must not recommend rollout/value/search-play policy from feature
  schema work alone.

## Copy-Ready Prompt For Coder

Read and follow `docs/spec/2026-06-14-cFp75-specs.md` exactly.

Start from current `dev`, then create and switch to:

```bash
git fetch origin --prune
git switch dev
git pull --ff-only origin dev
git switch -c codex/cFp75-search-consumer-contract-decision
```

Implement cFp75 as a search-consumer contract decision phase.

Hard boundaries:

- Do not implement third-ply execution.
- Do not re-observe benchmark roots.
- Do not rebuild sampled states.
- Do not run new benchmark command-execution probes.
- Do not add rollout, value, action ranking, best-action selection, win
  probability, reward targets, product AI wiring, or AI Lab runner buttons.
- Do not change gameplay behavior, engine rules, legal moves, AI policy
  behavior, sampler behavior, deck/catalog data, benchmark suite definitions,
  rating artifacts, or existing cFp68-cFp74 artifacts.
- Keep cFp75 docs/metadata-oriented. The main output is a decision note and a
  precise cFp76 contract.

Required outputs:

- Decision note:
  `docs/research/literature/ai/decisions/2026-06-14-cfp75-search-consumer-contract.md`
- Report:
  `audit/reports/2026-06-14-cFp75-report.md`
- Updated AI Lab metadata/tests.
- Updated benchmark docs, policy docs, roadmap, and `docs/PROJECT_STATE.md`.

The decision note must:

- pause blind third-ply execution;
- recommend `search-consumer-action-features-v0` for cFp76;
- define cFp76 source artifact contracts;
- define cFp76 output files;
- define cFp76 root/action feature schema categories;
- define cFp76 budget and artifact-size limits;
- define cFp76 hidden-info restrictions;
- define cFp76 non-claims and stop conditions.

Required checks:

```bash
npm test -- --run tests/components/gwent/authenticAiLabViewModel.test.ts
npm run typecheck
npm run lint
npm run build
npm run check:forbidden-imports
npm run check:project-state -- --base origin/dev
git diff --check
git diff --cached --check
npm run check:worktree-clean
```

Also run the direct exact-token hidden-info scan over the cFp75 decision note
and report. It should return no matches.

Commit and push when complete.
