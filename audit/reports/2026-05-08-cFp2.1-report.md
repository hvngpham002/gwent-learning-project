# cFp2.1 Report - Second Cowling ISMCTS Annotation Batch

## Summary

cFp2.1 continues the page-bounded `cowling2012ismcts` annotation after
explicit user permission. This phase read only PDF pp. 11-20 / article
pp. 129-138 and immediately appended page-grounded notes to the
existing annotation.

The annotation remains in progress. Final Gwent relevance, adaptation
risks, and AI-roadmap decision sections are still pending until the
full paper is read.

## Files Changed

- `docs/spec/2026-05-08-cFp2.1-specs.md`
- `docs/research/literature/ai/annotations/cowling2012ismcts/cowling2012ismcts.md`
- `docs/PROJECT_STATE.md`
- `audit/reports/2026-05-08-cFp2.1-report.md`

## Behavior Changed

None. This is a documentation and research annotation phase only.

No engine rules, legal moves, scoring, command execution, prompts,
round resolution, AI policy, simulation/export logic, UI, routes,
catalog data, deck builder, Card Studio, package scripts, CI, runtime
tests, or generated assets changed.

## Annotation Batch

Read only PDF pp. 11-20, covering:

- MO-ISMCTS;
- Lord of the Rings: The Confrontation game model, implementation, and
  experiments;
- Phantom (4, 4, 4) game model and experiments;
- Dou Di Zhu rules, implementation, ISMCTS/determinization comparison,
  branching-factor analysis, and commercial-AI comparison;
- the opening computation-time comparison through the LOTR:C
  decision-time results.

The annotation file now records `pages_read: "1-20"` and keeps these
sections pending:

- `Relevance To Gwent AI`
- `Risks For Adaptation`
- `AI-Roadmap Decision`

## Acceptance Criteria Status

- cFp2.1 spec exists: passed.
- Existing annotation was continued, not replaced: passed.
- Only PDF pp. 11-20 read in this phase: passed.
- Annotation file updated immediately after the second batch: passed.
- `pages_read` updated to `"1-20"`: passed.
- Final roadmap decision sections left pending until full read: passed.
- `docs/PROJECT_STATE.md` updated: passed.
- This report created: passed.
- Avoid AI/runtime implementation changes: passed.

## Deviations

None.

## Risks And Follow-Ups

- The annotation is still partial. Do not treat the pending roadmap
  sections as a method decision.
- The final Cowling batch remains unread: PDF pp. 21-25.
- Continue only after explicit user permission, then complete the final
  pending sections once the paper is fully read.

## Project State Update

`docs/PROJECT_STATE.md` now records cFp2.1 as the active Cluster F
handoff, adds a cFp2.1 ledger row, updates the Next Recommended Step to
the final bounded Cowling batch after explicit user permission, and
updates the active risk block to reflect `pages_read: "1-20"`.

## Checks Run

- `git diff --check`
- `npm run check:project-state -- --base HEAD~1`

Both checks passed.

## Recommended Next Step

Stop at the batch boundary. If the user explicitly replies
"continue", read only PDF pp. 21-25 of `cowling2012ismcts`, append
page-grounded notes immediately, complete the pending final sections if
the full paper has then been read, and stop again before starting the
next source.
