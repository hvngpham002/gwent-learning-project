# cFp7 Report - First Huang Invalid-Action-Masking Annotation Batch

## Summary

cFp7 starts the invalid-action-masking annotation for Huang and
Ontanon, "A Closer Look at Invalid Action Masking in Policy Gradient
Algorithms," using the bibliography-aligned citekey
`huang2022invalidmasking`.

This phase created the source package, normalized the existing local
ignored PDF to `source.pdf`, read only PDF pp. 1-10, and created the
first page-grounded annotation. The local source is ten PDF pages
total, but final Gwent relevance, adaptation risk, and AI-roadmap
decision sections remain pending until explicit finalization under the
annotation workflow.

## Files Changed

- `docs/spec/2026-05-09-cFp7-specs.md`
- `docs/research/literature/ai/results/2026-05-08/queue.md`
- `docs/research/literature/ai/source/huang2022invalidmasking/manifest.yaml`
- `docs/research/literature/ai/annotations/huang2022invalidmasking/huang2022invalidmasking.md`
- `docs/PROJECT_STATE.md`
- `audit/reports/2026-05-09-cFp7-report.md`

Local ignored source normalized:

- `docs/research/literature/ai/source/huang2022invalidmasking/source.pdf`

## Behavior Changed

None. This is a documentation and research annotation phase only.

No engine rules, legal moves, scoring, command execution, prompts,
round resolution, AI policy, simulation/export logic, UI, routes,
catalog data, deck builder, Card Studio, package scripts, CI, runtime
tests, or generated assets changed.

## Annotation Batch

Normalized the existing ignored local source from:
`docs/research/literature/ai/source/huang2022invalidmasking/huang2022invalidmasking.pdf`
to:
`docs/research/literature/ai/source/huang2022invalidmasking/source.pdf`.

Read only PDF pp. 1-10, covering:

- the motivation for masking invalid actions in large full discrete
  action spaces;
- the policy-gradient and masked-logit setup;
- Proposition 1's argument that the masked update is a valid policy
  gradient for the masked policy;
- the microRTS resource-harvesting environment, observation features,
  and eight-component action vector;
- the comparison regimes: invalid-action penalties, proper invalid
  action masking, naive masking, and train-with-mask / evaluate-without-
  mask;
- metrics for episodic return, invalid source selections, time to
  solve, and time to first reward;
- empirical results showing masking scaling better than invalid-action
  penalties as invalid-action density grows;
- the KL-divergence and instability concern for naive masking;
- related work on action embeddings, action elimination, and
  action-space shaping;
- appendix details for factored multi-discrete action generation, PPO
  implementation details, hyperparameters, frame skipping, and network
  architecture.

The annotation file now records:

- `pages_read: "1-10"`
- `status: "in-progress"`

The final sections remain pending:

- `Relevance To Gwent AI`
- `Risks For Adaptation`
- `AI-Roadmap Decision`

## Acceptance Criteria Status

- cFp7 spec exists: passed.
- Shared bibliography contains `huang2022invalidmasking`: passed.
- Source package manifest created: passed.
- Existing local PDF normalized to ignored `source.pdf` before reading:
  passed.
- Cluster F queue records in-progress status, local source status, and
  annotation path: passed.
- Only PDF pp. 1-10 read in this phase: passed.
- Annotation file updated immediately after the bounded batch: passed.
- `pages_read` set to `"1-10"`: passed.
- `status` remains `"in-progress"` while final decision sections are
  pending: passed.
- `docs/PROJECT_STATE.md` updated: passed.
- This report created: passed.
- Avoid AI/runtime implementation changes: passed.

## Deviations

The source is exactly ten PDF pages, so the first page-bounded batch
covers the full local source. The annotation still keeps final roadmap
decision sections pending until explicit finalization, matching the
workflow's conservative stop-after-batch discipline.

`lanctot2009monte`, `zinkevich2007regret`, and
`long2010understanding` remain in-progress from earlier Cluster F
phases. cFp7 starts the invalid-action-masking source because the user
explicitly requested `huang2022invalidmasking` annotations; it does not
finalize or synthesize the earlier pending annotations.

## Risks And Follow-Ups

- `huang2022invalidmasking` is not yet a completed annotation. Future
  specs should cite only the page-grounded factual notes until the
  final sections are completed and status is updated to `complete`.
- The paper grounds invalid action masking for policy-gradient
  algorithms, but cFp7 does not convert those claims into a Gwent
  observation/action schema, mask tensor format, training wrapper, or
  implementation acceptance criteria.
- The paper does not handle hidden-card information, information sets,
  equilibrium evaluation, deck-conditioned policies, or Gwent-specific
  action decomposition.
- The next research step should be explicit finalization of pending
  decision sections before using this annotation in an implementation
  spec.

## Project State Update

`docs/PROJECT_STATE.md` now records cFp7 as the active Cluster F
handoff, adds a cFp7 ledger row, updates the Next Recommended Step to
include `huang2022invalidmasking` among the pending finalization items,
and updates active risks to reflect that `huang2022invalidmasking` has
page-grounded notes but pending final decision sections.

## Checks Run

Both checks passed.

- `git diff --check`
- `npm run check:project-state -- --base HEAD~1`

## Recommended Next Step

After explicit finalization permission, complete the pending
`Relevance To Gwent AI`, `Risks For Adaptation`, and `AI-Roadmap
Decision` sections for `huang2022invalidmasking`, update the queue and
project state to complete status, then return to the remaining
in-progress `lanctot2009monte`, `zinkevich2007regret`, and
`long2010understanding` finalization work as directed.
