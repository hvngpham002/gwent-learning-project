# cFp17.1 Report - Second Brown Deep CFR Annotation Batch

## Summary

cFp17.1 continues the page-bounded `brown2019deepcfr` annotation after
explicit user permission. This phase read only PDF pp. 11-19 and
immediately appended page-grounded notes to the existing annotation.

The full local source is now read. The annotation remains in progress
because final Gwent relevance, adaptation risks, and AI-roadmap decision
sections still require explicit finalization permission.

## Files Changed

- `docs/spec/2026-05-09-cFp17.1-specs.md`
- `docs/research/literature/ai/annotations/brown2019deepcfr/brown2019deepcfr.md`
- `docs/PROJECT_STATE.md`
- `audit/reports/2026-05-09-cFp17.1-report.md`

## Behavior Changed

None. This is a documentation and research annotation phase only.

No engine rules, legal moves, scoring, command execution, prompts,
round resolution, AI policy, simulation/export logic, UI, routes,
catalog data, deck builder, Card Studio, package scripts, CI, runtime
tests, or generated assets changed.

## Annotation Batch

Read only PDF pp. 11-19, covering:

- Appendix A HULH and FHP rules;
- capped poker betting actions and FHP's two-round simplification;
- MCCFR sampled-block and external-sampling value derivation;
- Lemma 1 proof;
- K-external-sampling regret-bound proof;
- Deep CFR Theorem 1 proof with the value-network approximation-error
  term;
- Corollary 1 proof;
- the proof caveat that the clean memory argument assumes the advantage
  memory is not full;
- Appendix C's PyTorch network implementation with card embeddings,
  bet features, skip connections, normalization, and final action head.

The annotation file now records `pages_read: "1-19"` and keeps these
sections pending:

- `Relevance To Gwent AI`
- `Risks For Adaptation`
- `AI-Roadmap Decision`

## Acceptance Criteria Status

- cFp17.1 spec exists: passed.
- Existing annotation was continued, not replaced: passed.
- Only PDF pp. 11-19 read in this phase: passed.
- Annotation file updated immediately after the second batch: passed.
- `pages_read` updated to `"1-19"`: passed.
- Final roadmap decision sections left pending until explicit
  finalization: passed.
- `docs/PROJECT_STATE.md` updated: passed.
- This report created: passed.
- Avoid AI/runtime implementation changes: passed.

## Deviations

- cFp18 ReBeL annotation work already exists in the worktree as the
  previous active handoff. The user explicitly replied to the cFp17
  Brown Deep CFR continuation checkpoint, so cFp17.1 continues only
  `brown2019deepcfr` and does not touch `brown2020rebel`.
- The full local source is now read, but the annotation is intentionally
  still `status: "in-progress"` because the workflow requires explicit
  finalization permission before completing the final decision sections.

## Risks And Follow-Ups

- `brown2019deepcfr` is not yet a completed annotation. Future specs may
  cite only the page-grounded factual notes already written, not a
  completed Deep CFR roadmap decision.
- The Appendix B proof assumes sufficiently large advantage memory for
  the clean bound and does not formally handle the full-memory
  reservoir-sampling case.
- The Appendix C network is poker-specific: card groups plus betting
  features with a fixed action head. It does not provide a Gwent
  observation schema, variable legal-action target representation, or
  product AI policy.
- Deep CFR remains a substantial offline/reduced-game training
  candidate, not a near-term browser runtime implementation mandate.

## Project State Update

`docs/PROJECT_STATE.md` now records cFp17.1 as the active Cluster F
handoff, adds a cFp17.1 ledger row, records that `brown2019deepcfr` has
`pages_read: "1-19"` with final decision sections pending, and
preserves the cFp18 in-progress ReBeL state for `brown2020rebel`.

## Checks Run

- Passed: `git diff --check`
- Passed: `npm run check:project-state -- --base HEAD~1`

## Recommended Next Step

After explicit finalization permission, complete the pending
`Relevance To Gwent AI`, `Risks For Adaptation`, and `AI-Roadmap
Decision` sections for `brown2019deepcfr`, update the annotation status
to `complete`, update the queue and project state, and create the
finalization report.
