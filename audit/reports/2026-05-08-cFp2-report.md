# cFp2 Report - First Cowling ISMCTS Annotation Batch

## Summary

cFp2 starts the project literature annotation source of truth with the
first page-bounded batch for Cowling, Powley, and Whitehouse,
"Information Set Monte Carlo Tree Search." The phase creates the source
package, verifies the shared BibTeX entry, normalizes the local source
PDF to the workflow's `source.pdf` path, and writes page-grounded notes
for PDF pp. 1-10 / article pp. 120-128 only.

The annotation remains in progress. Final Gwent relevance, adaptation
risks, and AI-roadmap decision sections are intentionally pending until
the full paper is read under the batch protocol.

## Files Changed

- `.gitignore`
- `docs/spec/2026-05-08-cFp2-specs.md`
- `docs/research/literature/ai/source/cowling2012ismcts/manifest.yaml`
- `docs/research/literature/ai/annotations/cowling2012ismcts/cowling2012ismcts.md`
- `docs/PROJECT_STATE.md`
- `audit/reports/2026-05-08-cFp2-report.md`

Local-only ignored reading artifacts:

- `docs/research/literature/ai/source/cowling2012ismcts/cowling2012ismcts.pdf`
- `docs/research/literature/ai/source/cowling2012ismcts/source.pdf`

## Behavior Changed

None. This is a documentation and research annotation phase only.

No engine rules, legal moves, scoring, command execution, prompts,
round resolution, AI policy, simulation/export logic, UI, routes,
catalog data, deck builder, Card Studio, package scripts, CI, runtime
tests, or generated assets changed.

## Source Intake

- Verified `docs/research/bibliography/references.bib` contains the
  aligned `cowling2012ismcts` BibTeX entry.
- Found a local PDF at
  `docs/research/literature/ai/source/cowling2012ismcts/cowling2012ismcts.pdf`.
- Copied the local PDF to
  `docs/research/literature/ai/source/cowling2012ismcts/source.pdf`
  for the workflow's expected primary-source path while preserving the
  original local file.
- Added `.gitignore` coverage for local research PDFs under
  `docs/research/literature/ai/source/**/*.pdf`.
- Created `manifest.yaml` with `status: local-source-available` and
  `primary_source: source.pdf`.

## Annotation Batch

Read only PDF pp. 1-10, covering:

- White Rose submitted-version cover page;
- paper abstract and introduction;
- formal game and imperfect-information notation;
- determinization and PIMC background;
- simultaneous-move and chance-node handling background;
- start of the algorithm section through SO-ISMCTS + POM.

The annotation file records `pages_read: "1-10"` and keeps these
sections pending:

- `Relevance To Gwent AI`
- `Risks For Adaptation`
- `AI-Roadmap Decision`

## Acceptance Criteria Status

- cFp2 spec exists: passed.
- Source package manifest exists: passed.
- Shared bibliography contains `cowling2012ismcts`: passed.
- Raw `source.pdf` status checked before reading: passed.
- Only pp. 1-10 read in this phase: passed.
- Annotation file created immediately after the first batch: passed.
- Final roadmap decision sections left pending until full read: passed.
- `docs/PROJECT_STATE.md` updated: passed.
- This report created: passed.
- Avoid AI/runtime implementation changes: passed.

## Deviations

- The source folder already contained `cowling2012ismcts.pdf`. cFp2
  copied it to `source.pdf` instead of renaming it, so user-provided
  local source material was preserved.
- The local PDF is the White Rose submitted version. The manifest and
  annotation keep the IEEE citation metadata from the shared
  bibliography, while page anchors record both PDF pages and article
  pages where useful.

## Risks And Follow-Ups

- The annotation is partial. Do not treat the pending roadmap sections
  as a method decision.
- Future claims from this annotation must stay within pages already
  read unless the user continues the batch protocol.
- Continue with the next Cowling batch only after explicit user
  permission.
- Local source PDFs are intentionally ignored by git and must not be
  staged unless licensing is deliberately revisited.

## Project State Update

`docs/PROJECT_STATE.md` now records cFp2 as the active Cluster F
handoff, adds a cFp2 ledger row, updates the Next Recommended Step to
the next bounded Cowling batch only after explicit user permission, and
replaces the active cFp1.1 risks with cFp2 partial-annotation risks.

## Checks Run

- `git diff --check`
- `npm run check:project-state -- --base HEAD~1`

Both checks passed.

## Recommended Next Step

Stop at the batch boundary. If the user explicitly replies
"continue", read only the next `cowling2012ismcts` page batch, append
page-grounded notes immediately, update `pages_read`, and stop again
before the following batch.
