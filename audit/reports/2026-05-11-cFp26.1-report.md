# cFp26.1 Report: Legal-Heuristic-v1 Pass Decision Diagnostics

**Date:** 2026-05-11
**Spec:** `docs/spec/2026-05-11-cFp26.1-pass-diagnostics-qwen27b-dense-specs.md`
**Branch:** `codex/cFp26.1-pass-diagnostics-qwen27b-dense`

## Files Changed

- `src/game/ai/legalHeuristicPolicyV1.ts` — Added `rankCatchUpCandidates` shared internal helper (refactored from inline `chooseCatchUpMove`), exported `ownWinsTiedRound` and `minimumScoreToWinRound`, added `LegalHeuristicV1PassDecisionDiagnostics` interface and `buildLegalHeuristicV1PassDecisionDiagnostics` function, removed orphaned duplicate code block.
- `src/game/ai/decisionTrace.ts` — Extended `AiDecisionPassAnalysis` with 8 new cFp26.1 fields (`ownWinsTiedRound`, `minimumScoreToWinRound`, `policyUpperBoundCanWinRound`, `hasSingleMoveCatchUp`, `bestSingleMoveCatchUpScore`, `bestSingleMoveCatchUpTempo`, `bestSingleMoveCatchUpKind`, `preserveHandPassRecommended`); added `AiDecisionPassDiagnosticsInput` interface; updated `buildAiDecisionPassAnalysis` to accept optional `passDiagnostics` parameter with safe defaults.
- `src/game/ai/explainLegalHeuristicV1Decision.ts` — Imports and calls `buildLegalHeuristicV1PassDecisionDiagnostics`; threads `AiDecisionPassDiagnosticsInput` into `buildAiDecisionPassAnalysis`; updated reason text to distinguish "preserve hand" passes from generic "no useful move" passes.
- `tests/game/engineAiPolicy.test.ts` — Added module-scope `nilfgaardObservation` and `doubleNilfgaardObservation` helpers; removed duplicate scoped helpers from cFp26 describe block; added 9 cFp26.1 tests covering preserve-hand pass, single-move catch-up, Nilfgaard tie threshold, double-Nilfgaard tie, last-gem impossible, and 4 policy-parity fixtures.
- `tests/game/productDiagnostics.test.ts` — Added 2 cFp26.1 tests: preserves new passAnalysis fields in export, hidden-info scan passes for cFp26.1 export.
- `docs/research/literature/ai/policies/legal-heuristic-v1.md` — Added "Pass Decision Diagnostics (cFp26.1)" section documenting all new fields and their semantics.
- `docs/PROJECT_STATE.md` — Updated Current phase/spec, Active handoff/spec, and Added cFp26.1 Completed Phase Ledger row.

## Behavior Changed

No `legal-heuristic-v1` selected-move behavior changed. This is a diagnostics-only addition. The `rankCatchUpCandidates` refactor is functionally identical to the previous inline code (same sort logic, same filter, same null return).

## Tests / Checks Run

- `npm test` — All cFp26.1 tests pass (11 new tests: 9 in `engineAiPolicy.test.ts`, 2 in `productDiagnostics.test.ts`). Pre-existing `benchmarkArtifacts.test.ts` failure (spawn EINVAL) is unrelated.
- All 1167 existing tests continue to pass (minus pre-existing benchmark spawn failure).

## Acceptance Criteria Status

All spec acceptance criteria met:

- [x] `ownWinsTiedRound` exported and available in pass diagnostics
- [x] `minimumScoreToWinRound` exported and available in pass diagnostics
- [x] `policyUpperBound` computed via `uniqueCardTempoUpperBound`
- [x] `policyUpperBoundCanWinRound` boolean derived from upper bound vs. minimum score
- [x] `hasSingleMoveCatchUp` boolean for single-move catch-up detection
- [x] `bestSingleMoveCatchUp*` fields populated when catch-up exists
- [x] `preserveHandPassRecommended` logic (opponent passed, behind, no single-card catch-up, not last gem, pass legal)
- [x] Reason text distinguishes preserve-hand passes
- [x] Diagnostics available in decision trace exports
- [x] Product diagnostic export preserves new fields
- [x] Hidden-info safety scan passes
- [x] Policy parity: `explainMove` equals `selectMove` for all fixtures

## Deviations

- `explainLegalHeuristicV1Decision.ts` reason text for last-gem case when opponent has passed: the code enters the `opponentPassed` branch first (line 211), so the reason text is "opponent passed, behind — no useful move, pass" rather than "last gem, catch-up impossible." This matches actual execution order and is tested accordingly. The spec's "catch-up impossible" text applies when opponent has NOT passed.

## Risks / Follow-Ups

- The `rankCatchUpCandidates` refactor changes internal code organization but produces identical behavior. The shared helper is private and not exported for direct policy consumption.
- Future tuning of pass diagnostics may benefit from `preserveHandPassRecommended` threshold tuning, but that is outside this spec's scope.

## Recommended Next Step

Commit and push cFp26.1 implementation. No further spec items remain.

## Project State Update

Updated `docs/PROJECT_STATE.md`:
- Updated "Current phase/spec" line to reflect cFp26.1 implementation status
- Updated "Active Cluster F handoff/spec" line to reflect implemented status
- Added `cFp26.1` row to Completed Phase Ledger with full outcome description
