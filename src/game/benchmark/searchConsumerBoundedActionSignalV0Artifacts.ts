import { createHash } from "node:crypto";
import { mkdir, writeFile } from "node:fs/promises";
import { resolve } from "node:path";
import { scanSearchConsumerActionFeaturesV0TextForHiddenInfo } from "./searchConsumerActionFeaturesV0Artifacts";
import { CFP84_NON_CLAIMS } from "./searchConsumerBoundedActionSignalV0";
export const searchConsumerBoundedActionSignalV0ArtifactFiles=["manifest.json","summary.json","signal-config.json","root-evaluations.jsonl","action-signals.jsonl","skipped-roots.jsonl","failures-summary.json","report.md"] as const;
type Value=null|boolean|number|string|Value[]|{[key:string]:Value};
const clean=(v:unknown):Value=>v===null||["boolean","number","string"].includes(typeof v)?v as Value:Array.isArray(v)?v.map(clean):v&&typeof v==="object"?Object.fromEntries(Object.entries(v).filter(([,x])=>x!==undefined).sort(([a],[b])=>a.localeCompare(b)).map(([k,x])=>[k,clean(x)])):null;
const json=(v:unknown)=>`${JSON.stringify(clean(v),null,2)}\n`, jsonl=(v:readonly unknown[])=>v.map(x=>JSON.stringify(clean(x))).join("\n")+(v.length?"\n":""), sha=(v:string)=>createHash("sha256").update(v).digest("hex");
export interface Cfp84Artifacts { manifestJson:string; summaryJson:string; signalConfigJson:string; rootEvaluationsJsonl:string; actionSignalsJsonl:string; skippedRootsJsonl:string; failuresSummaryJson:string; reportMarkdown:string; }
export const serializeSearchConsumerBoundedActionSignalV0Artifacts=({suiteId,result,sourceArtifactReferences,sourceDictionaryHash}:{suiteId:string;result:any;sourceArtifactReferences:any[];sourceDictionaryHash:string}):Cfp84Artifacts=>{
 const rootEvaluationsJsonl=jsonl(result.rootRows),actionSignalsJsonl=jsonl(result.actionRows),skippedRootsJsonl=jsonl(result.skippedRows);
 const signalConfigJson=json({schemaVersion:"search-consumer-bounded-action-signal-v0-config-v1",phaseId:"cFp84",sampleCount:8,formula:{scoreSwing:"clamp(afterScoreDiff-beforeScoreDiff,-60,60) when phase and round unchanged; otherwise 0",handSwing:"clamp(handDiffDelta*8,-24,24)",gemSwing:"clamp(gemDiffDelta*25,-50,50)",terminalComponent:{win:100,loss:-100,draw:0,incomplete:0},immediateSignal:"clamp(sum,-200,200)"},roundingPolicy:"integer half-away-from-zero meanMilli",publicMetricAllowlist:["score","hand count","gem count","phase","round","match result"],explicitNonClaims:CFP84_NON_CLAIMS});
 const failuresSummaryJson=json({schemaVersion:"search-consumer-bounded-action-signal-v0-failures-v1",failureCounts:result.failureCounts});
 const summary=json({schemaVersion:"search-consumer-bounded-action-signal-v0-summary-v1",phaseId:"cFp84",suiteId,status:result.status,sourceConsistency:result.sourceConsistency,selectedRootCount:result.rootRows.length,selectedActionCount:result.actionRows.length,inheritedSkippedRootCount:result.skippedRows.length,expectedPairCount:result.expectedPairs,completedPairCount:result.completedPairs,failedPairCount:result.failedPairs,signalRange:result.signalRange,sampleSensitivityCounts:result.sensitivityCounts,sourceDictionaryHash,sourceArtifactReferences,hiddenInfoScanStatus:"clean",explicitNonClaims:CFP84_NON_CLAIMS});
 const reportMarkdown=`# Bounded Action Evaluation Signal v0\n\n- suite: ${suiteId}\n- status: ${result.status}\n- roots: ${result.rootRows.length}\n- actions: ${result.actionRows.length}\n- expected/completed pairs: ${result.expectedPairs}/${result.completedPairs}\n\nThis is a benchmark-only immediate public-state diagnostic, not expected value, win probability, reward, rollout, ranking, recommendation, selection, or product AI.\n`;
 const summaryJson=summary, hashes={"summary.json":sha(summaryJson),"signal-config.json":sha(signalConfigJson),"root-evaluations.jsonl":sha(rootEvaluationsJsonl),"action-signals.jsonl":sha(actionSignalsJsonl),"skipped-roots.jsonl":sha(skippedRootsJsonl),"failures-summary.json":sha(failuresSummaryJson),"report.md":sha(reportMarkdown)};
 const manifestJson=json({schemaVersion:"search-consumer-bounded-action-signal-v0-artifact-v1",phaseId:"cFp84",suiteId,relativeOutputPath:`docs/research/literature/ai/benchmark-results/${suiteId}/search-consumer-bounded-action-signal-v0/cFp84`,generatedAtTimestampPolicy:"omitted for determinism",files:searchConsumerBoundedActionSignalV0ArtifactFiles,status:result.status,sourceArtifactReferences,artifactHashes:hashes,explicitNonClaims:CFP84_NON_CLAIMS});
 return {manifestJson,summaryJson,signalConfigJson,rootEvaluationsJsonl,actionSignalsJsonl,skippedRootsJsonl,failuresSummaryJson,reportMarkdown};
};
export const searchConsumerBoundedActionSignalV0ArtifactHashes=(a:Cfp84Artifacts)=>Object.fromEntries([["manifest.json",a.manifestJson],["summary.json",a.summaryJson],["signal-config.json",a.signalConfigJson],["root-evaluations.jsonl",a.rootEvaluationsJsonl],["action-signals.jsonl",a.actionSignalsJsonl],["skipped-roots.jsonl",a.skippedRootsJsonl],["failures-summary.json",a.failuresSummaryJson],["report.md",a.reportMarkdown]].map(([k,v])=>[k,sha(v)]));
export const scanSearchConsumerBoundedActionSignalV0ArtifactsForHiddenInfo=(a:Cfp84Artifacts)=>scanSearchConsumerActionFeaturesV0TextForHiddenInfo(Object.values(a).join("\n"));
export const writeSearchConsumerBoundedActionSignalV0Artifacts=async({outDir,artifacts}:{outDir:string;artifacts:Cfp84Artifacts})=>{await mkdir(outDir,{recursive:true});const pairs=[["manifest.json",artifacts.manifestJson],["summary.json",artifacts.summaryJson],["signal-config.json",artifacts.signalConfigJson],["root-evaluations.jsonl",artifacts.rootEvaluationsJsonl],["action-signals.jsonl",artifacts.actionSignalsJsonl],["skipped-roots.jsonl",artifacts.skippedRootsJsonl],["failures-summary.json",artifacts.failuresSummaryJson],["report.md",artifacts.reportMarkdown]] as const;await Promise.all(pairs.map(([n,t])=>writeFile(resolve(outDir,n),t,"utf8")));return {outDir,files:pairs.map(([n])=>n)};};
